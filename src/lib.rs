mod tokenizer;
// use tokenizer::processAssignment;
// use uwl::StringStream;

#[cfg(test)]
mod tests {
    #[test]
    pub fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }

    #[test]
    pub fn test_assign() {
        //let inputString = uwl::StringStream::new("var x = 12");
    }
}
